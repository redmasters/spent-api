spring:
  application:
    name: spent-api

  datasource:
    url: 'jdpc:postgresql://wdrWSAV0VUaDGX52kuyf@containers-us-west-141.railway.app:7787/railway'
    username: '${PGUSER}'
    password: '${PGPASSWORD}'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
server:
  servlet:
    context-path: /api

springdoc:
  api-docs:
    path: /api-docs
